name: Nightly Match & Digest
on:
  schedule:
    - cron: '30 2 * * *'
  workflow_dispatch:
jobs:
  match:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: python nightly_match.py
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_KEY }}
